# Goal Brief — Complete Claude Code MCP server fleet readiness
Stamp: 2026-08-24 · Stream: S0 · Repo: `/Users/vmks/_IT_Projects/40_repos_cloned/claude-code-mcp-server` · Branch: `main`

## Intent / Why
Close every material repository-owned gap after Stage B reconciliation: certify the maintained fork proportionally, prove durable exact recovery and consumer impact, derive destination/steward only from authority, produce relocation rehearsal and rollback artifacts, and leave one clean canonical worktree with truthful terminal evidence.

## Scope
IN:
- Reconcile current Git identity, upstream fork delta, refs, local/ignored state, worktrees and stashes.
- Run dependency-backed build, unit, safe e2e/mock tests, audit and package checks when their runtime behavior is proven bounded; never invoke live Claude implicitly.
- Inspect package/runtime entry points, test quality, dependency/security state, documentation and release readiness; repair material repository-owned defects with tests.
- Scan bounded user-level MCP/editor/npm-link and fleet consumers without exposing values; repair only path consumers in scope and authorized by their owning authority.
- Derive destination and steward from manifests, decisions, existing operating groups and repository purpose; never treat ancestry or inference alone as authority.
- Produce typed relocation inventory, dry-run/rehearsal, source-hidden and byte-identical rollback plan/artifacts where authorized; execute a move only with an explicit approved destination.
- Refresh exact non-force remote refs or a verified complete bundle after authored changes; clean reproducible output after verification.

OUT:
- LogosForge, daemons, VPN, Spotlight mutation, paid/billable infrastructure, live Claude invocation without a proven isolated gate, secrets/opaque runtime data, unrelated repositories, force Git, new worktrees, or guessed placement.

## Acceptance Criteria (EARS, measurable)
- WHEN intake is refreshed, THE SYSTEM SHALL prove exact HEAD/tree/remote refs, fork/upstream disposition, ignored-state policy, one canonical worktree and zero stashes.
- WHEN native certification completes, THE SYSTEM SHALL show successful build/unit and every safe repository-native test population, production audit, package dry-run, static integrity and explicit disposition of excluded live tests.
- WHEN quality review completes, THE SYSTEM SHALL leave zero material repository-owned defects across implementation, tests, dependency posture, package payload and docs, with authored fixes committed and pushed non-forcefully.
- WHEN consumer discovery completes, THE SYSTEM SHALL account for bounded MCP/editor/npm-link/fleet consumers by path or package contract, repairing only authorized path-bound consumers and recording absence limits.
- WHEN placement review completes, THE SYSTEM SHALL cite exact destination/steward authority or record a resolving-blockers-qualified external decision; THE SYSTEM SHALL produce relocation typed inventory, rehearsal and rollback artifacts without inventing authority.
- WHEN closure completes, THE SYSTEM SHALL prove exact recovery for terminal refs, clean tracked state, accepted markers preserved, reproducible outputs removed, one worktree, zero stashes, current disk evidence and a final broad audit with zero ungated material work.

## Proof Method (transcript-provable)
- `git status --porcelain=v2 --branch`; `git worktree list --porcelain`; `git stash list`; `git ls-remote` for exact refs; `git bundle verify` and SHA-256 when bundle recovery is used.
- Repository package scripts after bounded runtime review: build, unit, safe e2e/mock, production audit and `npm pack --dry-run`; record exit codes and test populations.
- Bounded `rg -l` consumer scans over named configuration/control paths; npm global/link inspection without printing secret values.
- Relocation authority citations plus typed dry-run/rehearsal and rollback hashes; no actual move without an approved destination.
- Final `git diff --check`, connectivity check, status/ref equality, worktree/stash counts, generated-output absence, marker hashes and `df` evidence.

## Invariants (must not change)
- Preserve `.metadata_never_index`, `.DS_Store`, secrets, opaque settings, recovery bundles and runtime-owned state.
- No force operations, new worktrees, live service/Claude execution without a proven safe gate, or mutation outside the admitted repository and explicitly authorized path consumers.
- Existing user changes and concurrent `_claude-configs` work are not this task's mutation scope.

## Blocked = Concluded
Every criterion resolves PROVEN, FILED-BLOCKER or VOID. Before any blocker is accepted, invoke the same-name `resolving-blockers` skill, run its complete ladder, continue independent work and recheck. FILED-BLOCKER requires the pasted failure, named unavoidable external dependency, exact clearing action and three consecutive goal turns with unchanged fresh evidence. Difficulty, budget and disk pressure alone are not blockers.

### BLOCKER — placement-authority

- Status: FILED-BLOCKER candidate; first fresh goal-turn observation on 2026-08-24.
- Failure: exact `rg` over `_pdaa/workspace.yaml`, the campaign goal/payload/relocation authority and the decisions ledger returned no `claude-code-mcp-server` placement mapping; the proposed `_dev_tools` target is absent but absence is not authorization.
- Ladder: ground truth, troubleshooting KB, credential relevance, mint/self-hosted paths, standing grants and available interaction surfaces were checked in order. No source can create an authoritative fleet decision inside this repository task.
- External dependency: fleet coordinator placement decision.
- Exact clearing action: record the approved steward and exact collision-free operating-group destination for repository identity `vladimir-ks/claude-code-mcp-server`, then rerun the state-bound relocation rehearsal.
- Independent repository quality, consumer accounting, preservation and cleanup continue; do not move on inference.

## Scope Verdict
Tier L · approximately 25–45 turns · repository code/tests/docs plus bounded consumer and relocation artifacts. Verdict: oversized-single-goal; all remaining repository completion surfaces stay in one lineage.

## Verification / Definition of Done
Done only when every acceptance criterion is PROVEN, FILED-BLOCKER or VOID; a broad re-read of this brief and scan of touched code/tests/docs finds zero material ungated work; exact terminal refs and recovery are verified; and the same-name `retro-next-goal` skill emits the completion report and terminal/successor envelope.
